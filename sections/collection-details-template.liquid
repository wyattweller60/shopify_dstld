<div class="container-fluid">
  <div class="row">
    {% include 'headline-title', content: section.settings.headline_text, customClass: "pl-3" %}
    {% include 'description-input', content: section.settings.description_text, customClass: "" %}
    <div class="col-12 col-md-12">
      <div class="row">
        {% for product in all_products %}
          {% include 'collection-product', product: product %}
        {% endfor %}
      </div>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Collection Details",
  "settings": [
    {
      "type": "text",
      "id": "headline_text",
      "label": "Collection Title"
    },
    {
      "type": "richtext",
      "id": "description_text",
      "label": "Collection Description"
    }
  ],
  "blocks": [
    {
      "type": "product_grid_item",
      "name": "New Product",
      "settings": [
        {
          "type": "product",
          "id": "product",
          "label": "Select Product"
        }
      ]
    }
  ],
  "max_blocks": 20
}
{% endschema %}
